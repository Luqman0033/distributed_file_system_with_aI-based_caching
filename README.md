# distributed_file_system_with_aI-based_caching
In this project, we aim to build a mini file distribution system with AI-based caching. The file system will be similar to Google File System. This system consists of a master server and several chunk-based servers. The files are split into chunks, replicated and then stored on chunk servers. This system also implements fault tolerance.
